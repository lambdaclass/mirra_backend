version: '3.2'

services:
  postgres:
    container_name: server_lambda_game_backend_db
    image: postgres:16.1
    ports:
      - 127.0.0.1:5432:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=lambda_game_backend_dev
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - server_lambda_game_backend_data:/var/lib/postgresql/data/

volumes:
  server_lambda_game_backend_data:
