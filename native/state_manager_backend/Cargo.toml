[package]
name = "state_manager_backend"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
name = "state_manager_backend"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.30.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
